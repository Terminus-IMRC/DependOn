#ifndef DEF_INSIDE
	#define DEF_INSIDE
	//#define NDEBUG
	#include<assert.h>
	#include<stdio.h>
	#include<stdlib.h>
	#include<string.h>
	#include<memory.h>
	#include<ctype.h>
	#include <stdint.h>
	enum bool{
		false, true
	};
	#define pass(str)
	extern int X,Ceilings,OneLine;	//,rank,size,nemelen;
	extern long ple;
	extern void par(int*, long);
	extern int findx(char*);
	extern void separ(int*, char*);
#endif
